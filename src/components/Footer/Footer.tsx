import React, { FC } from 'react';
import styled from 'styled-components';

const Wrapper = styled.footer`
  background-color: #333;
  min-height: 180px;
`;

const Footer: FC = () => <Wrapper>Footer</Wrapper>;

export default Footer;
